# Example .env for Subscription-based Claude Code

# Container user
export DEV_USER=vincentb

# Backend selection
export CLAUDE_BACKEND=subscription

# No API key or AWS credentials needed
# You'll authenticate via OAuth when you first run Claude Code

# Optional: Model overrides
export CLAUDE_CODE_MAX_OUTPUT_TOKENS=4096
export MAX_THINKING_TOKENS=1024
